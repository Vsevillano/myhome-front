import { makeStyles } from "@mui/styles";

export const globalStyles = makeStyles(theme => ({
    mt10: {
        marginTop: '10px !important',
    },
    textCenter: {
        textAlign: 'center',
    }
}))