import { makeStyles } from "@mui/styles";

export const globalLayoutStyles = makeStyles(() => ({
    root: {      
      background: 'linear-gradient(174deg, rgba(106,172,228,1) 0%, rgba(255,255,255,1) 65%)',
      minHeight: 'calc(100vh - 60px)'
    }
}))